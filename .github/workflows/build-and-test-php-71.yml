name: Build and test - PHP 7.1
on:
  push:
  pull_request:
    types: [closed]
    branches: [main]

jobs:
  call-build-and-test:
    uses: ./.github/workflows/build-and-test.yml
    with:
      php_version: 7.1
      phpunit_version: 7.5.20
